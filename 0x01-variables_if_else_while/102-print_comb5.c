#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

int i;
int j;

for (i = '0'; i <= '98'; i++)
{
for (j = i + 1; j <= '99'; j++)
{

if (m == '0' && k == '0')
continue;

putchar(i);
putchar(' ');
putchar(j);


if (i == '98' && j == '99')
continue;

putchar(',');
putchar(' ');
}
}

putchar('\n');

/* your code goes there */
return (0);
}
